Master Branch

This branch is pushed via FTP to the hosting service.  Dev branches are merged with this branch periodically, when new content is added.

The Fiji shield logo is used in the header and navigation bar. Default dimensions in the navigation bar is 100x105 (WxH)